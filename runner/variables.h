#pragma once

/* The shared hardware sources include this historical game-variable header.
 * The LLE-first DKC3 probe addresses guest RAM through CpuState and does not
 * define title-specific C aliases. */
